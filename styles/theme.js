import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  typography: {
    fontFamily: ["Pacifio", "Roboto", "Arial", "sans-serif"].join(","),
  },
});

export default theme;
